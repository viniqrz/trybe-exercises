const toUpper = (str) => {
  return str.toUpperCase();
};

const firstLetter = (str) => {
  return str[0];
};

const concat = (str1, str2) => {
  return str1 + str2;
};

module.exports = { toUpper, firstLetter, concat };
